
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    function isSignedIn() {
      return request.auth != null;
    }
    function isOwner(uid) {
      return isSignedIn() && request.auth.uid == uid;
    }

    // Public Collections with Recursive Wildcard for robust querying
    match /quran_surahs/{document=**} {
      allow read: if true;
      allow write: if isSignedIn(); // Allow seeding by authenticated users
    }
    match /quran_ayahs/{document=**} {
      allow read: if true;
      allow write: if isSignedIn(); // Allow seeding by authenticated users
    }
    match /duas/{document=**} {
      allow read: if true;
      allow write: if isSignedIn(); // Allow seeding by authenticated users
    }

    // User Private
    match /users/{uid} {
      allow read, write: if isOwner(uid);
    }
    match /users/{uid}/bookmarks/{bookmarkId} {
      allow read, write: if isOwner(uid);
    }
    match /users/{uid}/favorite_duas/{duaId} {
      allow read, write: if isOwner(uid);
    }
    match /users/{uid}/learn_progress/progress {
      allow read, write: if isOwner(uid);
    }
  }
}
